// routes/qrCodeRoutes.js

const express = require('express');
const router = express.Router();
const { generateRouteQRCode } = require('../controllers/qrCodeController');

// Define the route for generating QR code
router.post('/generate-route-qr', generateRouteQRCode);

module.exports = router;
